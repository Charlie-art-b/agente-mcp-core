# --- Core ---
fastapi==0.115.0
uvicorn[standard]==0.51.0
pydantic==2.13.4
python-dotenv==1.0.1

# --- LLM del agente ---
# El SDK de Google Gen AI. El agente (Fase 4) corre sobre Gemini, que
# tiene capa gratuita para los modelos Flash.
google-genai==2.14.0

# --- MCP ---
# Independiente del proveedor del modelo: MCP es un estándar abierto, así
# que el servidor y las tools funcionan igual con Gemini, Claude o
# cualquier otro cliente compatible.
#
# 1.28.x trae FastMCP (mcp.server.fastmcp), que genera el JSON Schema de
# cada tool a partir de los type hints y el docstring. La 1.1.2 original
# solo tenía la API de bajo nivel, con el schema escrito a mano.
mcp==1.28.1

# --- Base de datos ---
sqlalchemy==2.0.35
psycopg2-binary==2.9.10
alembic==1.13.3

# --- RAG / Embeddings ---
# chromadb trae su propio modelo de embeddings liviano (ONNX) por defecto,
# no se necesita instalar sentence-transformers/PyTorch para arrancar.
chromadb==1.5.9

# --- Utilidades ---
httpx==0.28.1
tenacity==9.0.0

# --- Testing / Evaluación ---
pytest==8.3.3
pytest-asyncio==0.24.0

# --- Demo UI (opcional) ---
streamlit==1.39.0
